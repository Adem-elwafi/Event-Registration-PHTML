<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow border-0">
                <div class="card-header <?= isset($success) ? 'bg-success' : (isset($error) ? 'bg-danger' : 'bg-primary') ?> text-white text-center py-4">
                    <i class="bi bi-<?= isset($success) ? 'check-circle' : (isset($error) ? 'exclamation-triangle' : 'ticket-perforated') ?> display-4"></i>
                    <h3 class="mt-3 mb-0">
                        <?= isset($success) ? 'Inscription Confirmée' : (isset($error) ? 'Erreur' : 'Inscription') ?>
                    </h3>
                </div>
                <div class="card-body p-4 text-center">
                    <?php if (isset($success)): ?>
                        <div class="alert alert-success">
                            <i class="bi bi-check-circle-fill me-2"></i>
                            <?= $success ?>
                        </div>
                        <div class="mt-4">
                            <a href="client_dashboard.php" class="btn btn-primary me-2">
                                <i class="bi bi-house me-1"></i> Retour à mon espace
                            </a>
                            <a href="client_dashboard.php" class="btn btn-outline-primary">
                                <i class="bi bi-calendar3 me-1"></i> Voir d'autres événements
                            </a>
                        </div>
                    <?php elseif (isset($error)): ?>
                        <div class="alert alert-danger">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            <?= $error ?>
                        </div>
                        <div class="mt-4">
                            <a href="client_dashboard.php" class="btn btn-primary">
                                <i class="bi bi-arrow-left me-1"></i> Retour
                            </a>
                        </div>
                    <?php else: ?>
                        <div class="alert alert-info">
                            <i class="bi bi-info-circle me-2"></i>
                            Traitement de votre inscription...
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>