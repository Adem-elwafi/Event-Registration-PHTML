<div class="container py-4">
    <?php if ($event): ?>
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="h2 fw-bold text-primary"><?= htmlspecialchars($event['title']) ?></h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="index.php">Accueil</a></li>
                        <li class="breadcrumb-item"><a href="events.php">Événements</a></li>
                        <li class="breadcrumb-item active">Détails</li>
                    </ol>
                </nav>
            </div>
            <div class="btn-group">
                <a href="events.php" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left me-2"></i>Retour
                </a>
                <a href="update_event.php?event_id=<?= $event['event_id'] ?>" class="btn btn-warning">
                    <i class="bi bi-pencil me-2"></i>Modifier
                </a>
                <a href="inscrire_participant.php?event_id=<?= $event['event_id'] ?>" class="btn btn-success">
                    <i class="bi bi-person-plus me-2"></i>Inscrire
                </a>
            </div>
        </div>

        <div class="row">
            <!-- Event Info -->
            <div class="col-lg-8">
                <div class="card shadow border-0 mb-4">
                    <div class="card-header bg-primary text-white py-3">
                        <h5 class="mb-0"><i class="bi bi-info-circle me-2"></i>Informations de l'Événement</h5>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label fw-bold text-muted">Date</label>
                                <p class="fs-5">
                                    <i class="bi bi-calendar-date text-primary me-2"></i>
                                    <?= date('d/m/Y', strtotime($event['event_date'])) ?>
                                </p>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold text-muted">Lieu</label>
                                <p class="fs-5">
                                    <i class="bi bi-geo-alt text-success me-2"></i>
                                    <?= htmlspecialchars($event['lieu']) ?>
                                </p>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold text-muted">Prix</label>
                                <p class="fs-5">
                                    <i class="bi bi-tag text-warning me-2"></i>
                                    <?= number_format($event['prix'], 2, ',', ' ') ?> €
                                </p>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold text-muted">Statut</label>
                                <p class="fs-5">
                                    <?php if (strtotime($event['event_date']) < time()): ?>
                                        <span class="badge bg-secondary">
                                            <i class="bi bi-check-circle me-1"></i>Terminé
                                        </span>
                                    <?php else: ?>
                                        <span class="badge bg-success">
                                            <i class="bi bi-clock me-1"></i>À venir
                                        </span>
                                    <?php endif; ?>
                                </p>
                            </div>
                            <div class="col-12">
                                <label class="form-label fw-bold text-muted">Description</label>
                                <div class="border rounded p-3 bg-light">
                                    <?= nl2br(htmlspecialchars($event['description'] ?: 'Aucune description fournie')) ?>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Participants List -->
            <div class="col-lg-4">
                <div class="card shadow border-0">
                    <div class="card-header bg-info text-white py-3">
                        <h5 class="mb-0">
                            <i class="bi bi-people me-2"></i>
                            Participants (<?= count($participants) ?>)
                        </h5>
                    </div>
                    <div class="card-body">
                        <?php if (empty($participants)): ?>
                            <div class="text-center text-muted py-3">
                                <i class="bi bi-person-x display-6"></i>
                                <p class="mt-2 mb-0">Aucun participant inscrit</p>
                            </div>
                        <?php else: ?>
                            <div class="list-group list-group-flush">
                                <?php foreach($participants as $participant): ?>
                                <div class="list-group-item d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="mb-1"><?= htmlspecialchars($participant['name']) ?></h6>
                                        <small class="text-muted"><?= htmlspecialchars($participant['email']) ?></small>
                                    </div>
                                    <span class="badge bg-primary rounded-pill">
                                        <i class="bi bi-check"></i>
                                    </span>
                                </div>
                                <?php endforeach; ?>
                            </div>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>
    <?php else: ?>
        <div class="alert alert-danger text-center">
            <i class="bi bi-exclamation-triangle display-4"></i>
            <h3 class="mt-3">Événement non trouvé</h3>
            <a href="events.php" class="btn btn-primary mt-2">Retour aux événements</a>
        </div>
    <?php endif; ?>
</div>