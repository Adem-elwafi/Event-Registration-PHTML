<?php
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
?>
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <link rel="stylesheet" href="../style.css">
    <title>Gestion des Événements</title>
</head>
<body>
    <!-- Navigation -->
    <!-- Navbar is always visible. Logic below shows/hides items based on session and role. -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <!-- Brand: Always visible, links to dashboard depending on role -->
            <a class="navbar-brand" href="<?php echo (isset($_SESSION['role']) && $_SESSION['role'] === 'admin') ? 'index.php' : ((isset($_SESSION['role']) && $_SESSION['role'] === 'client') ? 'client_dashboard.php' : 'index.php'); ?>">
                <i class="bi bi-calendar-event me-2"></i>Gestion Événements
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <?php
                    // Show personalized navigation if user is logged in
                    if (isset($_SESSION['user_id']) && isset($_SESSION['role'])):
                        if ($_SESSION['role'] === 'admin'):
                    ?>
                        <!-- ADMIN Navigation -->
                        <li class="nav-item">
                            <a class="nav-link" href="index.php"><i class="bi bi-house me-1"></i> Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="events.php"><i class="bi bi-calendar3 me-1"></i> Événements</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="participants.php"><i class="bi bi-people me-1"></i> Participants</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="inscription.php"><i class="bi bi-clipboard-check me-1"></i> Inscriptions</a>
                        </li>
                    <?php else: ?>
                        <!-- CLIENT Navigation -->
                        <li class="nav-item">
                            <a class="nav-link" href="client_dashboard.php"><i class="bi bi-house me-1"></i> Mon Espace</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="inscription.php"><i class="bi bi-clipboard-check me-1"></i> Inscriptions</a>
                        </li>
                    <?php endif; ?>
                        <!-- User dropdown: shows username and logout -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle me-1"></i> <?php echo htmlspecialchars($_SESSION['username'] ?? 'Utilisateur'); ?>
                            </a>
                            <ul class="dropdown-menu">
                                <li><span class="dropdown-item-text">
                                    <i class="bi bi-shield-check me-2"></i>
                                    <?php echo ($_SESSION['role'] === 'admin') ? 'Administrateur' : 'Client'; ?>
                                </span></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="logout.php">
                                    <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                                </a></li>
                            </ul>
                        </li>
                    <?php else: ?>
                        <!-- Guest navigation: only login/register -->
                        <li class="nav-item">
                            <a class="nav-link" href="login.php"><i class="bi bi-box-arrow-in-right me-1"></i> Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="register.php"><i class="bi bi-person-plus me-1"></i> Inscription</a>
                        </li>
                    <?php endif; ?>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid p-0">
        <?php include $template . '.phtml'; ?>
    </div>

    <!-- Footer: Always visible, shows user role if logged in -->
    <footer>
        <div class="container text-center">
            <p class="mb-0">&copy; 2024 Gestion des Événements</p>
            <small class="text-muted">
                <?php if (isset($_SESSION['user_id']) && isset($_SESSION['role'])): ?>
                    Connecté en tant que <?= $_SESSION['role'] === 'admin' ? 'Administrateur' : 'Client' ?>
                <?php else: ?>
                    Visiteur
                <?php endif; ?>
            </small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <!-- If you have custom JS, ensure the path is correct. -->
    <script src="../assets/js/script.js"></script>
</body>
</html>