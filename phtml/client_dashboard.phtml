<div class="container py-4">
    <!-- En-tête -->
    <div class="row mb-4">
        <div class="col">
            <h1 class="h3 fw-bold text-primary">
                <i class="bi bi-person-circle me-2"></i>Mon Espace Client
            </h1>
            <p class="text-muted">Bienvenue <?= $_SESSION['username'] ?> ! Gérez vos inscriptions aux événements.</p>
        </div>
    </div>

    <!-- Mes inscriptions -->
    <div class="card shadow border-0 mb-4">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0"><i class="bi bi-ticket-perforated me-2"></i>Mes Événements Inscrits (<?= count($mes_inscriptions) ?>)</h5>
        </div>
        <div class="card-body">
            <?php if (empty($mes_inscriptions)): ?>
                <div class="text-center py-4 text-muted">
                    <i class="bi bi-calendar-x display-4"></i>
                    <p class="mt-3">Vous n'êtes inscrit à aucun événement</p>
                    <p class="text-muted">Inscrivez-vous à des événements dans la section ci-dessous</p>
                </div>
            <?php else: ?>
                <div class="row g-3">
                    <?php foreach($mes_inscriptions as $event): ?>
                    <div class="col-md-6 col-lg-4">
                        <div class="card event-card h-100">
                            <div class="card-header bg-success text-white">
                                <h6 class="card-title mb-0"><?= htmlspecialchars($event['title']) ?></h6>
                            </div>
                            <div class="card-body">
                                <p class="card-text small">
                                    <?= nl2br(htmlspecialchars(substr($event['description'], 0, 100))) ?>...
                                </p>
                                <div class="mb-2">
                                    <i class="bi bi-calendar-date text-primary"></i>
                                    <small class="text-muted"><?= date('d/m/Y', strtotime($event['event_date'])) ?></small>
                                </div>
                                <div class="mb-2">
                                    <i class="bi bi-geo-alt text-success"></i>
                                    <small class="text-muted"><?= htmlspecialchars($event['lieu']) ?></small>
                                </div>
                                <div class="mb-2">
                                    <i class="bi bi-tag text-warning"></i>
                                    <small class="text-muted"><?= number_format($event['prix'], 2, ',', ' ') ?> €</small>
                                </div>
                            </div>
                            <div class="card-footer bg-transparent">
                                <span class="badge bg-success">Déjà inscrit</span>
                            </div>
                        </div>
                    </div>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
        </div>
    </div>

    <!-- Événements disponibles -->
    <div class="card shadow border-0">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0"><i class="bi bi-calendar3 me-2"></i>Événements Disponibles (<?= count($evenements_disponibles) ?>)</h5>
        </div>
        <div class="card-body">
            <?php if (empty($evenements_disponibles)): ?>
                <div class="text-center py-4 text-muted">
                    <i class="bi bi-calendar-x display-4"></i>
                    <p class="mt-3">Aucun événement disponible pour le moment</p>
                    <p class="text-muted">Revenez plus tard pour découvrir de nouveaux événements</p>
                </div>
            <?php else: ?>
                <div class="row g-3">
                    <?php foreach($evenements_disponibles as $event): ?>
                    <div class="col-md-6 col-lg-4">
                        <div class="card event-card h-100">
                            <div class="card-header bg-primary text-white">
                                <h6 class="card-title mb-0"><?= htmlspecialchars($event['title']) ?></h6>
                            </div>
                            <div class="card-body">
                                <p class="card-text small">
                                    <?= nl2br(htmlspecialchars(substr($event['description'], 0, 100))) ?>...
                                </p>
                                <div class="mb-2">
                                    <i class="bi bi-calendar-date text-primary"></i>
                                    <small class="text-muted"><?= date('d/m/Y', strtotime($event['event_date'])) ?></small>
                                </div>
                                <div class="mb-2">
                                    <i class="bi bi-geo-alt text-success"></i>
                                    <small class="text-muted"><?= htmlspecialchars($event['lieu']) ?></small>
                                </div>
                                <div class="mb-2">
                                    <i class="bi bi-tag text-warning"></i>
                                    <small class="text-muted"><?= number_format($event['prix'], 2, ',', ' ') ?> €</small>
                                </div>
                            </div>
                            <div class="card-footer bg-transparent">
                                <a href="sinscrire_client.php?event_id=<?= $event['event_id'] ?>" 
                                   class="btn btn-sm btn-success">
                                    <i class="bi bi-person-plus"></i> S'inscrire
                                </a>
                                <button class="btn btn-sm btn-outline-primary" 
                                        data-bs-toggle="modal" 
                                        data-bs-target="#eventModal<?= $event['event_id'] ?>">
                                    <i class="bi bi-eye"></i> Détails
                                </button>
                            </div>
                        </div>

                        <!-- Modal pour les détails -->
                        <div class="modal fade" id="eventModal<?= $event['event_id'] ?>" tabindex="-1">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header bg-primary text-white">
                                        <h5 class="modal-title"><?= htmlspecialchars($event['title']) ?></h5>
                                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                    </div>
                                    <div class="modal-body">
                                        <p><strong>Description :</strong></p>
                                        <p><?= nl2br(htmlspecialchars($event['description'])) ?></p>
                                        <p><strong>Date :</strong> <?= date('d/m/Y', strtotime($event['event_date'])) ?></p>
                                        <p><strong>Lieu :</strong> <?= htmlspecialchars($event['lieu']) ?></p>
                                        <p><strong>Prix :</strong> <?= number_format($event['prix'], 2, ',', ' ') ?> €</p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
                                        <a href="sinscrire_client.php?event_id=<?= $event['event_id'] ?>" 
                                           class="btn btn-success">
                                            <i class="bi bi-person-plus"></i> S'inscrire
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>